enum Gender { none, male, female, nonBinary, transgender, genderFluid }

enum BodyType { none, athletic, curvy, slim, average, plusSize, muscular }

enum Ethnicity {
  none,
  black,
  asian,
  white,
  latino,
  middleEastern,
  native,
  pacific,
  other
}

enum Method {
  emailActivation,
  phoneActivation,
  emailRecovery,
  phoneRecovery,
}

enum HomePageType {
  before,
  after,
}

enum HomeContentType { before, after }
